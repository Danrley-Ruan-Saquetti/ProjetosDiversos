class Player {
    constructor(cardList, code) {
        this.cardList = cardList
        this.code = code
    }

    draw() {
        // for (let i = 0; i < this.cardList.length; i++) {
        //     switch (cards[i]) {
        //         case value:

        //             break;

        //         default:
        //             break;
        //     }
        // }
        // ctx.fillStyle =
    }
}

/*
SYMBOLS = {
    moreTwo: "+2",
    invert: "I",
    skip: ">",
    moreFour: "+4",
    touchColor: "X",
}
*/
